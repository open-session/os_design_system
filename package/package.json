{
  "name": "@opensession/design-system",
  "version": "0.1.3",
  "description": "Open Session design system — brand tokens, Tailwind preset, and a pre-compiled utility CSS for environments without a Tailwind build (v0.1). Components shipping in v0.2+. Distributed via Figma's npm registry for use in Figma Make kits.",
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "sideEffects": ["./dist/tokens/brand.css"],
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/open-session/os_design_system.git",
    "directory": "package"
  },
  "exports": {
    "./tokens/brand.css": "./dist/tokens/brand.css",
    "./tokens/utilities.css": "./dist/tokens/utilities.css",
    "./tokens/preset": {
      "types": "./dist/tokens/tailwind.config.d.ts",
      "import": "./dist/tokens/tailwind.config.js"
    }
  },
  "peerDependencies": {
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "build": "npm run build:clean && npm run build:types && npm run build:copy && npm run build:utilities",
    "build:clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build:types": "tsc -p tsconfig.build.json",
    "build:copy": "node build/copy-css.mjs",
    "build:utilities": "node build/generate-utilities.mjs",
    "verify-tag": "node build/verify-tag-version.mjs",
    "prepublishOnly": "npm run build"
  }
}
